name: Windows Clang Build (Bash)

on:
  push:
    branches: [ "undefined-symbol" ]
  pull_request:
    branches: [ "undefined-symbol" ]

jobs:
  build:
    runs-on: windows-latest

    defaults:
      run:
        shell: bash

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: true

    - name: Download LLVM
      run: |
        URL="https://github.com/llvm/llvm-project/releases/download/llvmorg-21.1.8/clang+llvm-21.1.8-x86_64-pc-windows-msvc.tar.xz"
        curl -L "$URL" -o llvm.tar.xz

    - name: Extract LLVM
      run: |
        mkdir -p llvm-dist
        tar -xf llvm.tar.xz -C llvm-dist --strip-components 1

    - name: Setup Path
      run: |
        LLVM_BIN_UNIX="$(pwd)/llvm-dist/bin"
        LLVM_BIN_WIN=$(cygpath -w "$LLVM_BIN_UNIX")
        
        echo "$LLVM_BIN_WIN" >> $GITHUB_PATH

        cd test_program
        mkdir build

    - name: Test Case 0
      if: always()
      run: |
        root=$(pwd)
        cd test_program/build
        cmake -S .. -B . \
          -G "Ninja" \
          -DCMAKE_C_COMPILER="$root/llvm-dist/bin/clang.exe" \
          -DCMAKE_CXX_COMPILER="$root/llvm-dist/bin/clang++.exe" \
          -DCMAKE_INSTALL_PREFIX:PATH="$root/install" \
          -DCMAKE_CXX_SCAN_FOR_MODULES=1 \
          -DCMAKE_EXPORT_COMPILE_COMMANDS=1 \
          -DCMAKE_BUILD_TYPE=Release \
          -DTEST_CASE=0
        cmake --build .
        cmake --install .
        $root/install/test_program.exe

    - name: Test Case 1
      if: always()
      run: |
        root=$(pwd)
        cd test_program/build
        cmake -S .. -B . \
          -G "Ninja" \
          -DCMAKE_C_COMPILER="$root/llvm-dist/bin/clang.exe" \
          -DCMAKE_CXX_COMPILER="$root/llvm-dist/bin/clang++.exe" \
          -DCMAKE_INSTALL_PREFIX:PATH="$root/install" \
          -DCMAKE_CXX_SCAN_FOR_MODULES=1 \
          -DCMAKE_EXPORT_COMPILE_COMMANDS=1 \
          -DCMAKE_BUILD_TYPE=Release \
          -DTEST_CASE=1
        cmake --build .
        cmake --install .
        $root/install/test_program.exe

    - name: Test Case 2
      if: always()
      run: |
        root=$(pwd)
        cd test_program/build
        cmake -S .. -B . \
          -G "Ninja" \
          -DCMAKE_C_COMPILER="$root/llvm-dist/bin/clang.exe" \
          -DCMAKE_CXX_COMPILER="$root/llvm-dist/bin/clang++.exe" \
          -DCMAKE_INSTALL_PREFIX:PATH="$root/install" \
          -DCMAKE_CXX_SCAN_FOR_MODULES=1 \
          -DCMAKE_EXPORT_COMPILE_COMMANDS=1 \
          -DCMAKE_BUILD_TYPE=Release \
          -DTEST_CASE=2
        cmake --build .
        cmake --install .
        $root/install/test_program.exe

    - name: Test Case 3
      if: always()
      run: |
        root=$(pwd)
        cd test_program/build
        cmake -S .. -B . \
          -G "Ninja" \
          -DCMAKE_C_COMPILER="$root/llvm-dist/bin/clang.exe" \
          -DCMAKE_CXX_COMPILER="$root/llvm-dist/bin/clang++.exe" \
          -DCMAKE_INSTALL_PREFIX:PATH="$root/install" \
          -DCMAKE_CXX_SCAN_FOR_MODULES=1 \
          -DCMAKE_EXPORT_COMPILE_COMMANDS=1 \
          -DCMAKE_BUILD_TYPE=Release \
          -DTEST_CASE=3
        cmake --build .
        cmake --install .
        $root/install/test_program.exe

    - name: Test Case 4
      if: always()
      run: |
        root=$(pwd)
        cd test_program/build
        cmake -S .. -B . \
          -G "Ninja" \
          -DCMAKE_C_COMPILER="$root/llvm-dist/bin/clang.exe" \
          -DCMAKE_CXX_COMPILER="$root/llvm-dist/bin/clang++.exe" \
          -DCMAKE_INSTALL_PREFIX:PATH="$root/install" \
          -DCMAKE_CXX_SCAN_FOR_MODULES=1 \
          -DCMAKE_EXPORT_COMPILE_COMMANDS=1 \
          -DCMAKE_BUILD_TYPE=Release \
          -DTEST_CASE=4
        cmake --build .
        cmake --install .
        $root/install/test_program.exe

    - name: Test Case 5
      if: always()
      run: |
        root=$(pwd)
        cd test_program/build
        cmake -S .. -B . \
          -G "Ninja" \
          -DCMAKE_C_COMPILER="$root/llvm-dist/bin/clang.exe" \
          -DCMAKE_CXX_COMPILER="$root/llvm-dist/bin/clang++.exe" \
          -DCMAKE_INSTALL_PREFIX:PATH="$root/install" \
          -DCMAKE_CXX_SCAN_FOR_MODULES=1 \
          -DCMAKE_EXPORT_COMPILE_COMMANDS=1 \
          -DCMAKE_BUILD_TYPE=Release \
          -DTEST_CASE=5
        cmake --build .
        cmake --install .
        $root/install/test_program.exe

    - name: Test Case 6
      if: always()
      run: |
        root=$(pwd)
        cd test_program/build
        cmake -S .. -B . \
          -G "Ninja" \
          -DCMAKE_C_COMPILER="$root/llvm-dist/bin/clang.exe" \
          -DCMAKE_CXX_COMPILER="$root/llvm-dist/bin/clang++.exe" \
          -DCMAKE_INSTALL_PREFIX:PATH="$root/install" \
          -DCMAKE_CXX_SCAN_FOR_MODULES=1 \
          -DCMAKE_EXPORT_COMPILE_COMMANDS=1 \
          -DCMAKE_BUILD_TYPE=Release \
          -DTEST_CASE=6
        cmake --build .
        cmake --install .
        $root/install/test_program.exe
